server:
  port: 7001


eureka:
  instance:
    hostname: localhost
  client:
    # 是否向注册中心注册本身服务
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka/